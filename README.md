# React / Next.js Setup

To run, do the following from the project root directory
Make sure you have node installed and are able to run npm command from the terminal

`cd campus-cravings`
`npm install`
`npm run dev`